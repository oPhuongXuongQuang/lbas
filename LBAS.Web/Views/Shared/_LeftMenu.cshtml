<!-- Left side column. contains the logo and sidebar -->
      <aside class="main-sidebar">
        <!-- sidebar: style can be found in sidebar.less -->
        <section class="sidebar">
          <!-- Sidebar user panel -->
          <div class="user-panel">
            <div class="pull-left image">
              <img src="~/Content/dist/img/user2-160x160.jpg" class="img-circle" alt="User Image">
            </div>
            <div class="pull-left info">
              <p>Alexander Pierce</p>
              <a href="#"><i class="fa fa-circle text-success"></i> Online</a>
            </div>
          </div>
          <!-- search form -->
          <form action="#" method="get" class="sidebar-form">
            <div class="input-group">
              <input type="text" name="q" class="form-control" placeholder="Search...">
              <span class="input-group-btn">
                <button type="submit" name="search" id="search-btn" class="btn btn-flat"><i class="fa fa-search"></i></button>
              </span>
            </div>
          </form>
          <!-- /.search form -->
          <!-- sidebar menu: : style can be found in sidebar.less -->
          <ul class="sidebar-menu">
            <li class="header">MAIN NAVIGATION</li>
            <li class="treeview @(ViewBag.MenuItem == 1 || ViewBag.MenuItem == 2 ? "active" : "")">
              <a href="#">
                <i class="fa fa-dashboard"></i> <span>Organization Management</span> <i class="fa fa-angle-left pull-right"></i>
              </a>
              <ul class="treeview-menu">
                <li class="@(ViewBag.MenuItem == 1 ? "active" : "")"><a href="/Corporation/Create"><i class="fa fa-circle-o"></i> New Corporation</a></li>
                <li class="@(ViewBag.MenuItem == 2 ? "active" : "")"> <a href="/Franchise/Create"><i class="fa fa-circle-o"> </i> New Franchise</a></li>
                 </ul>
            </li>
            <li class="treeview @(ViewBag.MenuItem == 3 ? "active" : "")">
              <a href="#">
                <i class="fa fa-files-o"></i>
                <span>User Administration</span>
                <i class="fa fa-angle-left pull-right"></i>
              </a>
              <ul class="treeview-menu">
                <li class="@(ViewBag.MenuItem == 3 ? "active" : "")"><a href="/User/Create"><i class="fa fa-circle-o"></i> New User</a></li>
               </ul>
            </li>
            <li class="treeview @(ViewBag.MenuItem == 4 || ViewBag.MenuItem == 5 || ViewBag.MenuItem == 6 ? "active" : "")">
              <a href="#">
                <i class="fa fa-files-o"></i>
                <span>Monitoring</span>
                <i class="fa fa-angle-left pull-right"></i>
              </a>
              <ul class="treeview-menu">
                <li class="@(ViewBag.MenuItem == 4 ? "active" : "")"><a href="/Site"><i class="fa fa-circle-o"></i> Site Management</a></li>
                <li class="@(ViewBag.MenuItem == 5 ? "active" : "")"><a href="/System"><i class="fa fa-circle-o"></i> System List</a></li>
                <li class="@(ViewBag.MenuItem == 6 ? "active" : "")"><a href="/Dashboard"><i class="fa fa-circle-o"></i> Bing Maps</a></li>
               </ul>
            </li>
        </ul>
        </section>
        <!-- /.sidebar -->
      </aside>

    